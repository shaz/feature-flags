# terraform

Infrastructure for fubo-flags: Cloud SQL (Postgres), the Managed Kafka
`fubo-flags.exposures` topic, Redis (change bus / cache), and GKE deployment of
the control plane and data plane with Workload Identity.

See [../DESIGN.md](../DESIGN.md) §9.
