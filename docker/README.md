# docker

Container definitions for the control plane and data plane. Each service has a
Makefile with `build` and `run` targets so images build locally before CI.

See [../DESIGN.md](../DESIGN.md) §9.
